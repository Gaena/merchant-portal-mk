# Базовый URL API. Пусто — запросы идут относительно origin, что верно для dev
# (прокси в vite.config.ts) и для прода за nginx на том же домене.
# Если фронтенд обслуживается отдельно, укажите адрес шлюза: https://api.example.az
VITE_API_BASE_URL=
