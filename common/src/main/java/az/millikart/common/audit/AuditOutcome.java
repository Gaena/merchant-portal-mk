package az.millikart.common.audit;

public enum AuditOutcome {
    SUCCESS,
    // Отказ в доступе: успешное изменение и так видно в данных, а отказ не видно больше нигде.
    DENIED,
    // Операцию отправили наружу, подтверждения не пришло: деньги у эквайера могли уйти, локальная
    // транзакция откатилась (P3-2). Писалось как SUCCESS и было неотличимо от чистой операции —
    // а эту запись разбирают руками. Пишет только AuditLogService.logUnresolved.
    UNRESOLVED
}
