# Merchant Portal (MP)

Портал мерчанта для приёма платежей через эквайринг MilliKart: платёжные ссылки
(Pay-by-Link, одно- и двухстадийные платежи, возвраты), справочники компаний и терминалов,
учётные записи с ролевым доступом и журнал аудита. Три Spring Boot-сервиса поверх общей
PostgreSQL плюс React-фронтенд.

## Модули

| Модуль | Что это | Порт |
|---|---|---|
| `common` | библиотека: security (JWT), журнал аудита, общие DTO и исключения | — |
| `auth` | вход/refresh/logout, пользователи и роли | 8081 |
| `directory` | компании, терминалы, чтение журнала аудита | 8082 |
| `pbl` | платёжные ссылки, транзакции, интеграция с TXPG | 8080 |
| `frontend` | React + Vite + MUI | 3000 (dev) |

## Запуск

Нужны JDK 21, Node 18+, PostgreSQL. Конфигурация — только через переменные окружения:
как минимум `DB_URL`, `DB_USERNAME`, `DB_PASSWORD` и `JWT_SECRET` (один и тот же секрет
для всех трёх сервисов); полный перечень с пояснениями — в [.env.example](.env.example)
и в `deployment_guide.md`. Значений в репозитории нет и быть не должно.

```bash
./gradlew :auth:bootRun
./gradlew :directory:bootRun
./gradlew :pbl:bootRun
```

Фронтенд:

```bash
cd frontend && npm install && npm run dev
```

Gradle-обёртка одна, в корне (`./gradlew`); модульных обёрток нет. Схему БД сервисы
накатывают сами через Liquibase, порядок старта не важен.

## Тесты

```bash
./gradlew test
```

Фронтенд собирается с проверкой типов (`tsc -b` внутри):

```bash
cd frontend && npm run build
```

## Документация

- `technical_handover.md` — технический паспорт системы: что реализовано, словарь журнала аудита (§4.4).
- `deployment_guide.md` — сборка, развёртывание, конфигурация окружения.
- `application_description.md` — архитектура, DTO, схемы БД, карта API.
- `problems.md` — известный техдолг и осознанно отложенные решения.
- `fix_plan.md` — трекер ревью-находок: решения (Р-NN), статусы, журнал работ.
- `AGENTS.md` — правила работы с кодовой базой (инварианты, грабли, тесты).
