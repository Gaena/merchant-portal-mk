package az.millikart.ecom.dto;

/**
 * Терминал, встречающийся в платежах мерчанта. Наполняет фильтр по терминалу.
 *
 * Имени терминала у провайдера в шлюзе нет — только номер (`tran.terminalid`), поэтому здесь
 * его тоже нет. Подставлять имя из нашего справочника нельзя: наши терминалы заведены для
 * платёжных ссылок, а эти принадлежат эквайрингу мерчанта и совпадать не обязаны.
 */
public record EcomTerminalResponse(
        String terminalId,
        long orderCount
) {
}
