rootProject.name = 'mp'

include 'common'
include 'auth'
include 'pbl'
include 'directory'
// Чтение эквайринговых платежей из схемы шлюза провайдера — своя база, свой пул, своё
// подключение. Отдельный сервис, а не модуль pbl: там наши собственные платежи и своя
// PostgreSQL, а здесь чужая операционная Oracle, в которую мы ходим только читать.
include 'ecom'

